<h1><%= t"views.user.index.list all users"%></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
      <%= user.email %>
      <%= user.avatar %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
