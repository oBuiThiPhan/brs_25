<%= form_for [@book, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit :Submit %>
  <%= f.hidden_field :user_id, value: current_user.id %>
<% end %>
