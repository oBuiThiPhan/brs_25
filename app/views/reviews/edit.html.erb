<h1><%= t"views.reviews.edit.editreview"%></h1>

<%= form_for [@book, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <%= f.label :content %>
  <%= f.text_field :content %>
  <%= f.submit :Update %>
<% end %>
